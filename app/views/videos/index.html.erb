My Odiun Videos

<div style="margin-bottom: 15px; margin-top: 10px;">
    <%= link_to "Add a Video", new_video_url %>
</div>

<div>
    <% @videos.each do |video| %>
        <div class="video-link"><%= link_to video.name, video_url(video) %></div>
    <% end %>
</div>